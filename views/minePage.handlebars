{{! this is a handlebars template with our list of rows}}

{{> header}}

<main id="board">
    {{#each row}}
        {{> row}}
    {{/each}}
</main>

{{#if modal}}
    {{> createSweeperModal}}
{{/if}}
